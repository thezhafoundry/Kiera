# Active Roadmap & Technical Debt

## Backlog
| Task | Priority | Status |
|---|---|---|
| **Commit the `RVCStreamingConverter` `connect_timeout` fix (10s → 45s).** Modal's `/ws` handshake measured 18.4s against a warm container even though `/health` answers in ~1s; at the old 10s default every connect attempt timed out, `_connection_loop` reconnected forever, and every input frame was silently dropped in the 500ms reconnect buffer — the session reported ready while producing zero converted audio. Fix is written in `backend/converters/rvc_stream.py` (see [[log]] 2026-07-29) but sitting uncommitted in the working tree as of this session. | P0 | Fixed locally 2026-07-29; not committed |
| **Revert `85bda43` — the desktop `send_lock` is live and delays the `ready` handshake.** It serializes every `websocket.send_*` in `DesktopAudioBridge.run`, which puts `send_json({"type":"ready"})` behind an unbounded queue of `relay_stats` tasks (one `asyncio.create_task` per converter stats event). Reproduced locally: hundreds of `stats` sent before `ready`. It was written for a concurrent-send race that does not exist — see [[log]] 2026-07-28 for why `socket.send() raised exception` never reaches our code. `d967620` (widening `send_stats` to catch `RuntimeError`) is inert and can be reverted alongside it. | P0 | Live in production since 2026-07-27; revert recommended, not yet done |
| **Desktop playout pacer: end-to-end confirmed working, cushion settled at 0.35s, and a real "stuck backlog" bug found+fixed 2026-08-02.** The `ea78c50` pacer is confirmed functioning on real desktop→VB-CABLE→WhatsApp sessions. Cushion tuning went 0.25s (breakup, commit history) → 0.5s (clean, no breakup) → 0.05s (untested experiment) → 0.35s (current, commit `5029194`, genuinely untested at time of writing) — see [[log]] for the full trail; re-verify 0.35s holds without breakup before trusting it further. Separately, a **second, more consequential bug** was found the same day: strict real-time-only pacing had no mechanism to reduce backlog once it existed — any early burst that pushed the buffer above the cushion became a **permanent** fixed delay for the rest of the call (reproduced live: steady ~4s lag for an entire call against a confirmed-warm GPU). Fixed with a bounded catch-up (commit `a9b2e64`): once backlog exceeds 0.75s, drain at 1.15x real time until back near 0.4s, then resume strict 1x. New test (`test_backlog_above_catchup_threshold_shrinks_over_time`) asserts backlog actually shrinks. **Not yet field-confirmed** — needs a live call where an early hiccup is deliberately induced or waited for, then watching "Playout buffer" visibly recover instead of staying elevated. | P0 | Pacer + cushion + catch-up all shipped 2026-08-02; field re-test of catch-up specifically still pending |
| **Desktop capture never got the 2026-07-08 double-noise-suppression fix until 2026-08-02.** `frontend/desktop/desktop.js`'s `getUserMedia` call left the browser's default `noiseSuppression`/`autoGainControl` on, stacking with the server-side `WebRTCNoiseSuppressor` — the same combination measured on the LiveKit path as −9dB at 6-8kHz / muffled output before that fix. Aligned with `frontend/app.js` (commit `5ddbd03`): browser NS/AGC now off, echo cancellation stays on. Motivated by an open-place/outdoor ambient-noise complaint — raising `NS_LEVEL` was considered and explicitly deferred in favor of this lower-risk fix first (see [[log]]). **Not yet field-confirmed** whether this alone resolves the ambient-noise complaint, or whether `NS_LEVEL` still needs raising afterward. | Medium | Fixed 2026-08-02; field verification pending |
| **Desktop latency panel undercounted total mouth-to-ear delay by omitting block-accumulation wait.** The "Estimated mouth-to-ear" stat summed only network RTT + GPU inference + current playout buffer size, showing ~100ms while a stopwatch measured ~2s on the same call. Root cause: `block_ms`+`context_ms` (the 400-720ms wait for enough audio to run inference at all, already present in every Modal `stats` message) was never read by the frontend. Fixed 2026-08-02 (commit `5ddbd03`): now shown as its own "Block accumulation wait" row and included in the total, in both the live panel and the new post-call summary. | Medium | Fixed 2026-08-02 |
| **`candidate_b` profile (400ms accumulation, 160/240/40/160) exported, TRT-compiled, and offline-verified clean 2026-08-02 — briefly deployed live, then reverted back to `baseline` (720ms) same day per explicit request.** ONNX export passed all three parity checks (`cosine=1.000000` for hubert/generator/rmvpe); TRT engine build completed; offline `main_chunked` replay reported no issues. This closes the "matching ONNX/TRT artifacts and quality evidence do not [exist]" gap noted below — they now do, isolated under `/root/rvc-models/profiles/candidate_b/`, `baseline`'s artifacts untouched. **Not yet A/B'd on a live call** before the revert happened, so live-call quality at 400ms remains unconfirmed either way. Re-deployable via `set RVC_STREAM_PROFILE=candidate_b && modal deploy modal_deploy/worker.py` without re-running export/compile. | High | Artifacts ready and offline-verified 2026-08-02; live A/B still open |
| ~~Modal worker is running plain ONNX-CUDA, not TensorRT.~~ **RESOLVED/STALE as of 2026-08-01/02**: live `/health` now reports `"engine":"trt"`, `"trt_cache":"hot"`, `cuda_available:true`, `NVIDIA L4` — confirmed twice (direct health check and a full `main_chunked` offline replay with steady ~50-53ms/block TRT timings). Whatever regressed it to `onnx-cuda` on 2026-07-28 is no longer the case; the earlier 20s inference-stall observation was likely tied to that same regressed state, not a standing risk. Leaving struck through rather than deleted in case the ONNX-CUDA fallback recurs. | — | Resolved, confirmed live 2026-08-01/02 |
| **Complete the macOS desktop/BlackHole acceptance run in real Chrome**: macOS registers `BlackHole 2ch` as a 2-in/2-out 48kHz CoreAudio device and the backend desktop relay passes with synthetic and spoken input. In Chrome, grant microphone permission, verify the page lists the physical MacBook microphone plus `BlackHole 2ch`, run **Test converted voice** (normal speakers; BlackHole not required), then select BlackHole and run **Start conversion** before validating WhatsApp Desktop's microphone routing. Do not use the Codex in-app browser: it did not complete the required Web Audio/device setup or expose the complete CoreAudio device list. | High | Backend + CoreAudio verified 2026-07-23; human microphone/Chrome/WhatsApp UI acceptance pending |
| **Harden desktop cold-start UX and readiness behavior**: a fresh `/api/desktop/audio` session waits up to 150s, then returns `{"code":"converter_unavailable"}`. On 2026-07-23 Modal spent ~5 minutes waiting for an `ap-southeast` L4 before ~34s model/TRT startup; another cold run failed the desktop gate, while a direct `/health` warm-up later took ~94s and the identical desktop run then became ready in 1.856s. Add an explicit warming/progress flow and decide between longer bounded readiness, controlled retry, `RVC_KEEPWARM=1` cost, or the pending broader-AP routing experiment. Preserve fail-closed behavior. | P0 | Reproduced live 2026-07-23; warm conversion passes, cold capacity remains unreliable |
| **Fix the standalone RVC benchmark launcher**: `.venv/bin/python scripts/rvc_stream_benchmark.py` fails with `ModuleNotFoundError: backend`; `.venv/bin/python -m scripts.rvc_stream_benchmark` works from the repo root. Make the documented/direct form self-locating or document module invocation, then add a launcher smoke test. | Medium | Reproduced 2026-07-23 |
| **Clean up post-auth-removal local-launcher/logging remnants**: operator auth is globally disabled, but `scripts/run_local.py` still describes/sets `KEIRA_LOCAL_LAUNCHER`/`KEIRA_LOCAL_BIND_HOST` markers that `backend/main.py` no longer reads. The server also always prints “RVC keep-warm loop started” even when `RVC_KEEPWARM` is unset/default-off and the task immediately returns. Remove or rename the vestigial auth wording/markers and log the actual keep-warm state. | Medium | Open, identified 2026-07-23 after merge `6e284bd` |
| **RVC-first Gate R1 — finish production baseline**: metadata/profile/model fingerprint and dynamic client timing are deployed and the direct WebSocket baseline is captured; run the same benchmark from Render Singapore, then one warm staff-PSTN spectral/listen test before changing block sizes. | P0 | Partial: Modal v11 verified 2026-07-16; Render/PSTN measurements pending |
| **Resolve RVC duration loss**: the 9.6s baseline returned 9.38854s (-211.46ms) despite zero queue/connection drops. Separate expected SOLA tail holdback from additional time compression and fix before promoting a lower-latency profile. | P0 | Open, measured 2026-07-16 |
| **A/B Modal routing from the real origin**: compare legacy `fastapi_app` (`ap-southeast` compute/default US routing) with `fastapi_app_ap` (Mumbai routing/broad AP; first container landed Tokyo) from Render Singapore. Keep Render on legacy until the result is recorded. | P0 | Experimental endpoint deployed; production switch not made |
| **Repair the nominal GPU warm-up**: Modal startup logs `No module named 'infer.lib.infer_pack.modules.F0Predictor'`; startup continues and TRT becomes ready, but the intended inference warm-up is skipped. | High | Open, observed on v10/v11 startup |
| **Candidate B artifacts now exist (see the 2026-08-02 row above) — this row is superseded, kept for the still-open Gate R1/PSTN ordering question.** `candidate_b` geometry (160/240/40/160) has matching ONNX/TRT artifacts and passed offline quality evidence as of 2026-08-02, ahead of Gate R1 completing — the original "benchmark only after Gate R1" sequencing was not followed (deployed briefly, then reverted, same day). Whether that ordering violation matters is untested; re-derive from [[log]] before assuming either "fine" or "must redo after Gate R1." Candidate C (120/200/20/120) is still not implemented. | High | Candidate B artifacts ready 2026-08-02; sequencing vs. Gate R1 unresolved |
| **Pause LLVC training/deployment**: the prototype was fully removed from `main`/this tree (2026-07-16) and set aside on branch `codex/llvc-pilot`; do not generate a 360-hour teacher corpus. Reassess zero-shot streaming VC only after RVC optimization, re-deriving scope from that branch. | P0 | Decision recorded 2026-07-16; code removed from main 2026-07-16 |
| Decide whether git history containing the old Render MCP bearer token requires scrubbing. The credential itself was user-confirmed rotated on 2026-07-16. | Medium | Rotation complete; history decision open |
| **Control plane is unauthenticated and this is now LIVE in production**: the `KEIRA_CONTROL_TOKEN` bearer-auth gate on operator routes was deliberately removed 2026-07-19 (see [[log]]) — every operator/dashboard route and the call-events WS are open to anyone who can reach the public Render URL. Committed `1f1fb5d` and pushed to `origin/main` by the user directly (2026-07-19 16:22 IST) — despite this session's explicit instruction not to push — which per Render `autoDeploy: commit` means it has already redeployed. Twilio signature validation and `CORS_ORIGINS` restriction are unaffected. Re-add auth before this stays public any longer than intended. | P0 | Regressed by request 2026-07-19; committed and pushed to origin/main, live on Render |
| **Fix bot lifecycle and SIP gating**: outbound now prepares, joins, then dials; inbound waits for worker readiness and confirmed isolation; verify both with a live call. | P0 | Implemented locally; live verification pending |
| **Make `/api/setup` non-destructive**: it now reuses/creates only Keira-named resources and reports conflicts without deleting unrelated trunks/rules. | P0 | Implemented locally; live verification pending |
| `TWILIO_AUTH_TOKEN` on Render appears stale/invalid — `POST /api/setup`'s Twilio-webhook-config step 401'd while its LiveKit-trunk step succeeded. Refresh the token in Render's env vars, then re-run `/api/setup`. | High | Open, identified 2026-07-03 (see [[subsystem-notes]]) |
| Confirm the retried outbound call succeeds end-to-end after the SIP trunk recreation and new trunk ID. | High | Open, identified 2026-07-03 (see [[subsystem-notes]]) |
| `RVC/` folder: ~195 files are committed despite the folder being intended as gitignored. Remove unneeded tracked files with an explicit `git rm -r --cached RVC/` only after confirming the TRT shims and local changes. | Medium | Open, identified 2026-07-03; unblocked 2026-07-07 |
| **Turn `DEBUG_SAVE_AUDIO` back OFF** (`modal_deploy/worker.py`'s `_web_function_options` env dict → `"0"` + `modal deploy`) once the 2026-07-19 word-clarity investigation (see [[log]]) is done. Currently **ON and live** — re-enabled 2026-07-19 for that diagnostic session (commit `610f306`, deployed and pushed to `origin/main`), capturing per-call `in16k`/`out48k` WAV pairs to `rvc-models` volume. Leaving it on indefinitely isn't the intended default. | High | ON in production since 2026-07-19; needs an explicit off-switch once diagnostics wrap up |
| Verify the 2026-07-08 voice identity/clarity fix; pitch is field-confirmed but input clarity regressed on later calls. | High | Partially resolved 2026-07-14 |
| **`/api/setup` must preserve the inbound `;edge=singapore` pin** on future reconciliation. | Medium | Open, identified 2026-07-08 |
| **`RVC_MALE_PITCH_SHIFT=7` is calibrated to one agent**; continue validating adaptive pitch behavior and the audible transition. | Medium | Open, identified 2026-07-08 |
| Verify the standing playout-buffer/bigger-block fix eliminates part-by-part audio on a live call. | High | Open, identified 2026-07-03 |
| Re-verify TRT live deployment, C4 offline A/B WAVs, and C5 listen test. **Now blocking a real bug, not just routine verification**: `rand_ini` fix (commit `3f28944`, 2026-07-29) needs a fresh ONNX export + TRT engine rebuild + `modal deploy` before it's live — confirm via `/health`'s `trt_cache`/`model_version` and a real listen test for the "ummmmm bzzzzzzz" tonal buzz specifically (see [[log]] 2026-07-29 evening entry). | High | Open, identified 2026-07-07; rand_ini fix committed 2026-07-29, deploy/verification still pending |
| Avoid Render `autoDeploy: commit` killing in-flight calls mid-test; add drain/graceful shutdown later. | Medium | Open, identified 2026-07-02 |
| Compile RNNoise / get `webrtc-noise-gain` MSVC build working on Windows. | Low | Open |
| **Adaptive pitch lock**: one-second prior→locked interpolation is implemented, deployed in Modal v11, and covered by tests; listen-test the transition and revalidate `RVC_TARGET_F0=208`. | High | Live listening follow-up open |
| **Input audio muffled again**: perform a genuine hard refresh before the next field call; static JS/CSS now receive no-cache headers locally. | High | Local cache-control fix implemented 2026-07-15; live hard-refresh verification pending |
| **Playout buffer overshoots and gulp-drains** during long continuous speech. The 2026-07-15 bounded-100ms-chunk fix alone did not resolve it (reconfirmed on a 2026-07-20 field call: `playout_buffer_bytes` still swinging up to ~400ms). Root cause: `_run_playout_consumer` had no wall-clock pacing of its own, only LiveKit backpressure. Re-implemented the 2026-07-16 `next_publish_time` real-time pacer (previously built, reviewed, deployed, then reverted same-day 2026-07-17 with no stated reason — see [[log]]) on 2026-07-21, plus a **newly-found fix the original attempt didn't have**: real-time pacing means the consumer routinely holds real backlog when a call ends, and the old teardown (`_run_conversion_stream`'s `finally`) cancelled it immediately, silently truncating that queued tail audio. Fixed with a cancellation-safe flush (publish an in-flight chunk before honoring cancellation; flush any remainder still sitting in `_playout_buffer` after the consumer stops) — caught by the existing `test_playout_buffer_smooths_bursty_converter_output` test once its fake converter was pointed at the pacer. | High | Local fix re-implemented 2026-07-21 (with teardown-flush fix added); live listen test pending — do not deploy without one, per the 2026-07-17 lesson |

## Resolved
| Task | Resolved | Notes |
|---|---|---|
| Modal/Render region mismatch | 2026-07-03 | Render confirmed live in Singapore, colocated with Modal. |
| Modal worker had no `max_containers` cap | 2026-07-03 | Cap added; current approved two-call configuration is `max_containers=2` per edge, one active stream per container. |
| Modal worker authentication deployment | 2026-07-16 | `rvc-api-key` secret exists; authenticated production WebSocket readiness succeeded on v10/v11. |
| GPU-side pitch/gender auto-detection unreliable | 2026-07-03 | Removed from the dashboard path; current UI now exposes the selected profile. |
| FAISS index re-read per inference call | 2026-07-03 | Cached via a `faiss.read_index` monkeypatch. |
| SIP audio isolation protobuf field-name bug | 2026-07-03 | Field fixed and confirmed in sampled logs; stronger fail-closed gating remains under P0 verification. |
| RTP packet loss on the LiveKit→Twilio SIP leg | 2026-07-08 | Re-pointed trunk/origination to Singapore edge; field verification continues. |
| Converted voice mismatch and muffled output | 2026-07-08 | Pitch and double-noise-suppression fixes shipped; later input-clarity regression remains open above. |

## Known Tech Debt
- Buffering/playout logic has been reverted and reimplemented repeatedly; treat timing edits as high-risk and rerun the spectral latency test (procedure in [[subsystem-notes]]).
- `scripts/rvc_stream_benchmark.py` now automates converter-path regression measurement, but it is not in CI and cannot replace the manual browser/PSTN mouth-to-ear spectral test.
- `RVC/` is vendored third-party WebUI code and should remain outside normal runtime scans.
